<% var typeOfComponent = props.isPure ? 'PureComponent' : 'Component' %>import React, { <%= typeOfComponent %> } from 'react';
import { StyleSheet, Text, View } from 'react-native';
<% if (props.isConnected) { %>import { connect } from 'react-redux';
import { createStructuredSelector } from 'reselect';<% } %>

const styles = StyleSheet.create({
    container: {},
});

interface Props {};

class <%= props.name %> extends <%= typeOfComponent %><Props> {
    // Defaults for props
    static defaultProps = {};

    render() {
        return (
            <View style={styles.container}>
                <Text><%= props.name %> <%= typeOfComponent %></Text>
            </View>
        );
    }
}

<% if (props.isConnected) { %>const mapStateToProps = createStructuredSelector({
});
const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(<%= props.name %>);<% } else { %>
export default <%= props.name %>;<% } %>
