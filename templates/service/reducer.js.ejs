import { createSlice } from '@reduxjs/toolkit';

interface <%= props.name %> {
    // TODO
}

interface <%= props.name %>ServiceState {
    data: <%= props.name %> | {},
    errors: string | null,
}

const initialState: <%= props.name %>ServiceState = {
    data: {},
    errors: null,
}

const <%= props.name %>Service = createSlice({
    name: '<%= props.name %>Service',
    initialState,
    reducers: {
        got<%= props.name %>s: (state: <%= props.name %>ServiceState, action: ReduxAction) => {
            state.data = action.payload;
            state.errors = null;
        },
        get<%= props.name %>sFailed: (state: <%= props.name %>ServiceState, action: ReduxAction) => {
            state.errors = action.payload;
        }
    }
});

export default <%= props.name %>Service;
