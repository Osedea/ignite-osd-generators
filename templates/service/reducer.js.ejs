import { createSlice } from '@reduxjs/toolkit';

interface <%= props.singularName %> {
    // TODO
}

interface <%= props.name %>ServiceState {
    list: Array<<%= props.singularName %>>,
    errors: string | null,
}

const initialState: <%= props.name %>ServiceState = {
    list: [],
    errors: null,
}

const <%= props.name %>Service = createSlice({
    name: '<%= props.name %>Service',
    initialState,
    reducers: {
        got<%= props.name %>: (state: <%= props.name %>ServiceState, action: ReduxAction) => {
            state.list = action.payload;
            state.errors = null;
        },
        get<%= props.name %>Failed: (state: <%= props.name %>ServiceState, action: ReduxAction) => {
            state.errors = action.payload;
        }
    }
});

export default <%= props.name %>Service;
