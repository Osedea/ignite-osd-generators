import React from 'react';
<% if (props.isConnected) { %>import { connect } from 'react-redux';
import { createStructuredSelector } from 'reselect';
<% } %>import { StyleSheet, Text, View } from 'react-native';

const styles = StyleSheet.create({
    container: {},
});

type Props = {};

const <%= props.name %> = (props: Props) => (
    <View style={styles.container}>
        <Text><%= props.name %> Functional Component</Text>
    </View>
);

<% if (props.isConnected) { %>const mapStateToProps = createStructuredSelector({
});
const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(<%= props.name %>);<% } else { %>
export default <%= props.name %>;<% } %>
