const add = async function (context) {
    // This just adding generators
};

/**
 * Remove yourself from the project.
 */
const remove = async function (context) {
    // Got nothing to remove
};

module.exports = {
    add,
    remove,
};
